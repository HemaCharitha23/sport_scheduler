<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Available Sessions</title>
</head>
<body>
    <h1>Available Sports Sessions</h1>
    
    <% if (sessions.length === 0) { %>
        <p>No sessions available.</p>
    <% } else { %>
        <ul>
            <% sessions.forEach(session => { %>
                <li>
                    <strong>Sport:</strong> <%= session.sport.name %> <br>
                    <strong>Venue:</strong> <%= session.venue %> <br>
                    <strong>Date:</strong> <%= session.dateTime %> <br>
                    <strong>Status:</strong> <%= session.status %> <br>
                    
                    <% if (session.status === 'active') { %>
                        <form action="/join-session/<%= session.id %>" method="POST">
                            <button type="submit">Join</button>
                        </form>
                    <% } else { %>
                        <strong>Cancelled: <%= session.cancelReason %></strong>
                    <% } %>
                </li>
            <% }); %>
        </ul>
    <% } %>

    <br>
    <a href="/playerdashboard">Back to Dashboard</a>
</body>
</html>
